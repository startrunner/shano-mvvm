<Window x:Class="ShanoMVVM.DemoApplication.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:ShanoMVVM.DemoApplication.ViewModels"
        xmlns:shano="clr-namespace:ShanoMVVM;assembly=ShanoMVVM"
        mc:Ignorable="d vm"
        Title="ShellView" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=vm:ShellViewModel, IsDesignTimeCreatable=False}">
    <Window.Resources>
        <shano:DesignTimeConverter x:Key="DTConverter"/>
    </Window.Resources>
    <StackPanel Orientation="Vertical">
        <TextBox Name="xTxt1" Text="{Binding Alpha, Mode=OneWay, Converter={StaticResource DTConverter}, ConverterParameter=PeshoGosho}"/>
        <TextBox Name="xTxt2" Text="{Binding Beta, Mode=OneWay}"/>
        <TextBox Name="xTxt3" Text="{Binding Gamma, Mode=OneWay, TargetNullValue=12}"/>
        <Button Content="{Binding Alpha, Converter={StaticResource DTConverter}, ConverterParameter=lala}"/>
    </StackPanel>
</Window>
