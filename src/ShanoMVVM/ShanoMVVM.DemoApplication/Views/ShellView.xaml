<Window x:Class="ShanoMVVM.DemoApplication.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sh="http://source.exposed/xmlns/shano-mvvm"
        xmlns:vm="clr-namespace:ShanoMVVM.DemoApplication.ViewModels"
        mc:Ignorable="d vm"
        Title="ShellView" Height="450" Width="800"
        Name="XWindow"
        >
    <StackPanel Orientation="Vertical">
        <TextBox Text="{Binding Alpha, Mode=OneWay}"/>
        <TextBox Text="{Binding Beta, Mode=OneWay}"/>
        <TextBox Text="{Binding Gamma, Mode=OneWay}"/>
    </StackPanel>
</Window>
